# **Basics & Traversal**
---

 ## **Largest ELement & smallest in an Array**  {***}

> - ### Brute Force - sort array and nth or n-1 term, arr[0] orr arr[1]
> - ### Better - make arr[0] largest or smallest and compare over all element
> - ### Optimal - we take a prioity queue and push all the item in it and its top is largest


### Brute Force Approach (smallest Also Largest)

>- We use Here Sorting Techniques (Bubbol Sort or Any Other)
>- And the Last Element Of Sorted Array is Largest Element



```
class Solution {
public:
    int largest(int arr[], int n) {
        auto swapel = [](int &a , int &b) {
            int temp = a;
            a = b;
            b = temp;
        };
        
        for(int i = 0 ; i < n ; i++) {
            for(int j = 0 ; j < n - i - 1 ; j++) {
                if(arr[j] > arr[j+1]) {
                    swapel(arr[j], arr[j+1]);
                }
            }
        }
        return arr[n-1]; // return arr[0];
    }
};
```

---

## Better Solution 

> - Take Arr[0] as a Largest elemnt
> - Comapre it For largest Over All element and ``` If(arr[i] > largest) largest = arr[i]```
> - C

 
 ```
 class Solution {
    public : 

    int largest (int arr[] , int n){
        int largest = arr[0];
        for(int i = 0 ; i < n ; i++){
            if(largest < arr[i]){
                largest = arr[0];
            }
        }

        return largest;
    }
 }

 ```

 ## Optimal Solution

 > - Here We will use Priority Queue
 > - Here the maximum or largest element always in top 

 ```
#include <iostream>
#include <queue>
#include <vector>
using namespace std;

class Solution {
public:

int largest( vector<int> &arr ){
    
   int n = arr.size();
   priority_queue<int> pq;

   for(int i = 0 ; i < n ; i++){
       pq.push(arr[i]);
   }

   return pq.top();
}

};  


```

## **Second Largest ELement & Second Smallest in an Array**  {***}


> - ### Brute Force - sort array and nth or n-2 term, arr[1] orr arr[2]
> - ### Better - make arr[0] largest or smallest and compare over all element and 
```
   if (arr[i] > largest) {
       secondLargest = largest;
       largest = arr[i];
  } else if (arr[i] != largest && arr[i] > secondLargest) {
       secondLargest = arr[i];
   } 
```
> - ### Optimal - we take a prioity queue and push all the item in it and its top is largest and pop then it is second large

```
  int first = pq.top(); pq.pop();
  while (!pq.empty() && pq.top() == first) pq.pop(); // skip duplicates
   return pq.empty() ? -1 : pq.top();
```


### Brute Force Approach (secondsmallest Also secondLargest)

```
int secondlands(vector <int> &arr ){
    int n = arr.size()
    for(int i = 0 ; i < n ; i++){
        for(int j = 0 ; j < n-i-1 ; j++){
            if(arr[j+1] < arr[j]){
                swap(arr[j] , arr[j+1])
            }
        }
    }
    return {(arr[n-1] , arr[n-2] , arr[0] , arr[1])}
}

```
### Better Approach ()

> - initialize largest secondl = INT_MIN; smallest seconds = INT_MAX;
> - starts loop compare - ```  if (arr[i] > largest) {
       secondsmallest = smallest;
       largest = arr[i];
  } else if (arr[i] != largest && arr[i] > secondLargest) {
       secondLargest = arr[i];
   }  ``` it is for largest and secondlargest
> - for smallest seconds ``` if(arr[i] < smallest){ secondlarge = largest; largest = arr[i]} if(arr[i] != smallest && arr[i] < seconds) { seconds = arr[i]} ```




```
int secondlands(vector<int> &arr){
    int n = arr.size();
    if(n < 2) return {-1, -1, -1, -1};

     int largest = INT_MIN;
     int smallest= INT_MAX;
     int seconds = INT_MAX;
     int secondl = INT_MIN;

    for(int i = 0; i < n  ; i++){
        if(arr[i] > largest){
            secondl = largest;
            largest = arr[i];
        }
        if(arr[i] != largest && arr[i] > secondl ) {
            secondl = arr[i];
        }
        

        if(arr[i] < smallest) {
                seconds = smallest;
                smallest = arr[i];
            }
        if(arr[i] != smallest && arr[i] < seconds ){
                seconds = arr[i];
            }

            if (secondl == INT_MIN) secondl = -1;
            if (seconds == INT_MAX) seconds = -1; 

         
    }

     return {largest , seconds , secondl , largestl};

}
```
- ### Optimal Approach ()
 ---

 > - 

 ```
 int find ( vector <int> &arr){
     int n = arr.size();
    if (n < 2) return {-1, -1, -1, -1};
    
    set<int> uniqueElements(arr.begin(),  arr.end());

    if(uniqueElements.size() < 2 ) return {-1,-1,-1,-1}

    priority_queue<int> maxHeap;

    priority_queue<int, vector<int>, greater<int>> minHeap;

    for(int val : uniqueElements) {
        maxHeap.push(val);
        minHeap.push(val);
    }

    int largest = maxHeap.top(); maxHeap.pop();
    int secondLargest = maxHeap.top();

    int smallest = minHeap.top(); minHeap.pop();
    int secondSmallest = minHeap.top();

    return {largest, secondLargest, smallest, secondSmallest};
   
    
 }
 ```
